.git
.venv
**/__pycache__
**/*.pyc
Janis_all_jars.zip
output
*.csv

# Never referenced by the Dockerfile, which COPYs specific paths rather than
# `COPY . .`. Excluded so they are not shipped to the daemon as build context
# on every build.
.ruff_cache
tests
docs
*.md
