"""Plotly Dash user interface, hosted on the FastAPI ASGI application.

Nothing in this package performs scientific calculation - every number shown
comes from `backend.services.*`, `backend.charts`, or `backend.jar_runner`,
which are the same functions the `/api/*` routes call.
"""
